[package]
name = "cloudflare-completions"
version = "0.1.0"
edition = "2021"

[dependencies]
extism-pdk = "1.1.0"
serde = { version = "1.0.202", features = ["derive"] }
serde_json = "1.0.117"
anyhow = "1.0"

[lib]
crate_type = ["cdylib"]
